import 'package:flutter/material.dart';

class AppColors {
  // Brand & Primary Colors
  static const primaryColor = Color(0xFF1F1F1F); // Dark Base
  static const accentColor = Color(0xFFE63946); // Energetic Red
  static const secondaryColor = Color(0xFF457B9D); // Steel Blue

  // Backgrounds
  static const background = Color(0xFF121212); // Deep Black
  static const cardBackground = Color(0xFF1E1E1E); // Slightly lighter than base
  static const scaffoldBackground = Color(0xFF0F0F0F); // Pure dark background

  // Text Colors
  static const primaryText = Color(0xFFFFFFFF); // White
  static const secondaryText = Color(0xFFB0B0B0); // Soft Gray
  static const mutedText = Color(0xFF808080); // Muted gray for descriptions

  // Buttons
  static const buttonColor = Color(0xFFE63946); // Red
  static const buttonText = Color(0xFFFFFFFF); // White

  // Status
  static const success = Color(0xFF2ECC71); // Green
  static const warning = Color(0xFFF39C12); // Orange
  static const error = Color(0xFFE74C3C); // Red

  // Icons
  static const iconActive = Color(0xFFE63946);
  static const iconInactive = Color(0xFF808080);

  // Input Fields
  static const inputBackground = Color(0xFF2A2A2A);
  static const inputBorder = Color(0xFF3C3C3C);
  static const focusedBorder = Color(0xFFE63946);

  // Dividers
  static const divider = Color(0xFF303030);

  // Shadows and overlays
  static const overlay = Color(0x66000000);
  static const shadow = Color(0x33000000);

  // Gradients
  static const List<Color> primaryGradient = [
    Color(0xFFE63946),
    Color(0xFF1F1F1F),
  ];
}
